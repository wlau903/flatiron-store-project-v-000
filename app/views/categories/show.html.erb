<h1><%= @category.title %></h1>

<ul>
<% @category.items.each do |item| %>
    <li><%= link_to item.title, item_path(item) %></li>
    <li><%= "Price: $#{item.price.to_f/100}" %></li>
    <%= button_to 'Add to Cart', line_items_path(item) %>
<% end %>
</ul>
